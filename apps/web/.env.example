# Same-origin via app/api/[...path] proxy (works with localhost and LAN Network URL).
NEXT_PUBLIC_API_BASE_URL=/api
# Nest target for the /api proxy route (server-side only)
API_PROXY_TARGET=http://127.0.0.1:3001
