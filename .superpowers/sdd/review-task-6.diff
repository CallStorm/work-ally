# Review edba4b4b5027a59f8bbc813d5852bbd11bbcbb6f..155455bfb60b00e6ee97d6504673bceb9849e82e
## Commits
155455b feat(web): scaffold handbook workbench page
## Stat
 apps/web/src/app/workbench/apps/handbook/page.tsx |  6 ++  apps/web/src/components/handbook/handbook-app.tsx | 78 +++++++++++++++++++++++  apps/web/src/components/handbook/types.ts         | 21 ++++++  3 files changed, 105 insertions(+)
## Diff
diff --git a/apps/web/src/app/workbench/apps/handbook/page.tsx b/apps/web/src/app/workbench/apps/handbook/page.tsx new file mode 100644 index 0000000..f52df48 --- /dev/null +++ b/apps/web/src/app/workbench/apps/handbook/page.tsx @@ -0,0 +1,6 @@ +'use client'; +import { HandbookApp } from '@/components/handbook/handbook-app'; + +export default function HandbookPage() { +  return <HandbookApp />; +} diff --git a/apps/web/src/components/handbook/handbook-app.tsx b/apps/web/src/components/handbook/handbook-app.tsx new file mode 100644 index 0000000..ee0a9c5 --- /dev/null +++ b/apps/web/src/components/handbook/handbook-app.tsx @@ -0,0 +1,78 @@ +'use client'; + +import { useEffect, useRef, useState } from 'react'; +import { ApiError, apiFetch } from '@/lib/api'; +import type { +  CategorySelection, +  HandbookCategory, +  HandbookNote, +} from './types'; + +function notesPath(selection: CategorySelection, query: string) { +  const params = new URLSearchParams(); +  if (selection !== 'all') params.set('categoryId', selection); +  const q = query.trim(); +  if (q) params.set('q', q); +  const qs = params.toString(); +  return qs ? `/apps/handbook/notes?${qs}` : '/apps/handbook/notes'; +} + +export function HandbookApp() { +  const [categories, setCategories] = useState<HandbookCategory[]>([]); +  const [notes, setNotes] = useState<HandbookNote[]>([]); +  const [selection] = useState<CategorySelection>('all'); +  const [selectedNoteId] = useState<string | null>(null); +  const [query] = useState(''); +  const [loading, setLoading] = useState(true); +  const [loadError, setLoadError] = useState<string | null>(null); +  const fetchIdRef = useRef(0); + +  useEffect(() => { +    const requestId = ++fetchIdRef.current; +    setLoading(true); +    setLoadError(null); + +    void (async () => { +      try { +        const [nextCategories, nextNotes] = await Promise.all([ +          apiFetch<HandbookCategory[]>('/apps/handbook/categories'), +          apiFetch<HandbookNote[]>(notesPath(selection, query)), +        ]); +        if (requestId !== fetchIdRef.current) return; +        setCategories(nextCategories); +        setNotes(nextNotes); +      } catch (err) { +        if (requestId !== fetchIdRef.current) return; +        if (err instanceof ApiError && err.status === 403) { +          setLoadError(err.message || '鏃犳潈浣跨敤鎵嬪唽搴旂敤'); +        } else { +          setLoadError(err instanceof Error ? err.message : '鍔犺浇鎵嬪唽澶辫触'); +        } +      } finally { +        if (requestId === fetchIdRef.current) setLoading(false); +      } +    })(); +  }, [selection, query]); + +  return ( +    <div className="handbook-app"> +      {loadError && ( +        <div className="handbook-app__error" role="alert"> +          {loadError} +        </div> +      )} +      {loading && <div className="handbook-app__empty">鍔犺浇涓€?/div>} +      <div className="handbook-app__cols"> +        <aside className="handbook-app__tree"> +          鍒嗙被锛坽categories.length}锛?+        </aside> +        <section className="handbook-app__list"> +          绗旇锛坽notes.length}锛?+        </section> +        <section className="handbook-app__editor"> +          {selectedNoteId ? `绗旇 ${selectedNoteId}` : '缂栬緫'} +        </section> +      </div> +    </div> +  ); +} diff --git a/apps/web/src/components/handbook/types.ts b/apps/web/src/components/handbook/types.ts new file mode 100644 index 0000000..f218466 --- /dev/null +++ b/apps/web/src/components/handbook/types.ts @@ -0,0 +1,21 @@ +export type HandbookCategory = { +  id: string; +  name: string; +  parentId: string | null; +  sortOrder: number; +  createdAt: string; +  updatedAt: string; +}; + +export type HandbookNote = { +  id: string; +  title: string; +  bodyMd: string; +  categoryId: string | null; +  pinned: boolean; +  createdAt: string; +  updatedAt: string; +}; + +/** 宸︽爮閫変腑锛氬叏閮?| 鏈垎绫?| 鏌愬垎绫?id */ +export type CategorySelection = 'all' | 'uncategorized' | string;
